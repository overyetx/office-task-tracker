<Window x:Class="OfficeTaskTracker.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Height="220" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    
    <Border Background="#1a1a2e" CornerRadius="12" BorderThickness="1" BorderBrush="#2a3555">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Color="Black" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Custom TitleBar -->
            <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TitleText" Text="Warning" FontSize="13" FontWeight="SemiBold" Foreground="#888" VerticalAlignment="Center" Margin="16,0,0,0" FontFamily="Segoe UI"/>
                
                <Button Grid.Column="1" Content="✕" Width="44" Background="Transparent" BorderThickness="0" Foreground="#AAA" Cursor="Hand" Click="CloseWindow_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="Transparent" CornerRadius="0,11,0,0">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Row="1" Margin="24,10,24,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="IconText" Text="ℹ️" FontSize="36" FontFamily="Segoe UI Emoji" Foreground="White" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock x:Name="MessageText" Grid.Column="1" Text="Message will appear here." TextWrapping="Wrap" FontSize="14" Foreground="#E0E0E0" FontFamily="Segoe UI" VerticalAlignment="Center"/>
            </Grid>

            <!-- Buttons Area -->
            <Border Grid.Row="2" Background="#16213e" CornerRadius="0,0,12,12" BorderThickness="0,1,0,0" BorderBrush="#2a3555">
                <StackPanel x:Name="ButtonsPanel" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,16,0">
                    <!-- Buttons will be dynamically added here -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
